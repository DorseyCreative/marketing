# marketing

Static marketing pages for Dorsey Creative Concepts, served via GitHub Pages.

Each piece lives in its own directory with an unguessable slug so it can be shared
by link without being listed or indexed. Pages carry `noindex` and the site
carries a blanket-disallow `robots.txt`.

Directory slugs are intentionally not documented here.
